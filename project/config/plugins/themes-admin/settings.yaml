# enabled: true or false to disable the plugin
enabled: true

# Themes Admin plugin priority
priority: 90

# Themes Admin navigation
navigation:
  dropdown: []
  actions: 
    - properties:
        target: "_blank"
        href: "{{ getBaseUrl() }}"
        icon:
          set: bootstrap
          name: globe
   
    